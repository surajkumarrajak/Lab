pipeline{
    agent any
    stages {
        stage ('initialize'){
            steps {
                sh '''
                    echo "PATH = ${PATH}"
                    echo "M2_HOME = ${M2_HOME}"
                    echo "Initializing"
                ''' 
            }
        }
        stage ('Build') {
            steps {
                sh """
                echo "Building Artefacts"
                """
            }
        }
    }
}
